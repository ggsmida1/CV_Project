# 字符残缺检测系统 —— 代码技术文档

---

## 一、整体架构

本项目基于 **Qt 5 + OpenCV 4** 开发，实现了一套字符/印刷区域残缺检测系统。

### 1.1 项目文件结构

```
opencv-last/
├── main.cpp                     程序入口
├── mainwindow.h / mainwindow.cpp 主窗口（界面交互 + 业务流程编排）
├── mainwindow.ui                Qt Designer 界面设计文件（XML 格式）
├── style.qss                    Qt 样式表（美化界面）
├── resources.qrc                Qt 资源文件
├── include/                     头文件目录（.h）
│   ├── types.h                  核心数据结构定义
│   ├── detector.h               检测算法函数声明
│   ├── imageutil.h              图像处理工具函数声明
│   ├── configmanager.h          配置文件读写声明
│   └── resultmanager.h          检测结果管理声明
├── src/                         源文件目录（.cpp）
│   ├── detector.cpp             检测算法实现（核心算法）
│   ├── imageutil.cpp            图像处理工具函数实现
│   ├── configmanager.cpp        配置文件读写实现
│   └── resultmanager.cpp        检测结果管理实现
└── samples/                     示例数据
    ├── templates/               模板图片
    ├── configs/                 ROI 配置文件（JSON 格式）
    └── test_images/             待测图片
```

### 1.2 设计思想

采用**模块化、分层解耦**的设计：

- **数据层**（`types.h`）：定义系统通用的数据结构，与业务逻辑无关
- **算法层**（`detector.cpp`、`imageutil.cpp`）：纯图像处理逻辑，不依赖 GUI
- **持久化层**（`configmanager.cpp`、`resultmanager.cpp`）：配置读写与结果输出
- **界面层**（`mainwindow.cpp`）：用户交互与流程调度，调用上述模块

### 1.3 模块调用关系

```
          ┌─────────────┐
          │  mainwindow │ ← 界面交互与流程调度中心
          │   .cpp       │
          └──┬──┬──┬──┬──┘
             │  │  │  │  │
      ┌──────┘  │  │  │  └──────────────┐
      ▼         ▼  ▼  ▼                 ▼
┌─────────┐ ┌─────┐ ┌────────────┐ ┌────────────┐
│  types  │ │image│ │  detector  │ │  config    │
│   .h    │ │util │ │   .cpp     │ │  manager   │
│         │ │.cpp │ │ （核心算法）│ │   .cpp     │
│ 数据结构 │ │工具 │ │            │ │ JSON读写   │
└─────────┘ └─────┘ └────────────┘ └────────────┘
                                             │
                                             ▼
                                  ┌────────────────────┐
                                  │  resultmanager.cpp │
                                  │（表格+CSV+清空）    │
                                  └────────────────────┘
```

### 1.4 检测流程总览

对待测图片执行一次完整检测，内部会经过以下步骤：

```
原始待测图像
    ↓
┌────────────────────┐
│ detectSkewAndCorrect│ ← 倾斜矫正（Canny + HoughLinesP + warpAffine）
└────────────────────┘
    ↓
┌────────────────────┐
│  templateMatch      │ ← 模板粗定位（NCC 全图搜索）
└────────────────────┘
    ↓
┌────────────────────┐
│    对每个 ROI:       │
│  ┌ alignROI()      │ ← ROI 精对齐（粗定位附近局部搜索）
│  └ detectCharacterDefect() ← NCC 残缺判定
└────────────────────┘
    ↓
结果图像（红色/绿色矩形框标注） + 检测结果表格
```

---

## 二、核心数据结构 —— types.h

定义系统中传递的两类核心数据对象。所有模块共享这两个结构体。

### 2.1 ROIRect —— 检测区域

描述模板图像上的一个矩形检测区域（Region of Interest）。

```
┌── 模板图像（template image）────────────────────┐
│                                                   │
│    ┌── rect (x,y,w,h) ──┐                       │
│    │   编号区            │ ← templateImage（从模板裁剪出的子图）│
│    │  (用户框选的区域)    │                       │
│    └────────────────────┘                       │
│                                                   │
└───────────────────────────────────────────────────┘
```

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` | `int` | 区域编号，从 1 开始递增 |
| `rect` | `cv::Rect` | 矩形坐标：左上角 (x,y) + 宽高 (width,height)，单位像素 |
| `name` | `QString` | 区域名称，如"区域1"、"编号区" |
| `templateImage` | `cv::Mat` | 从模板图像中 `rect` 位置裁剪出的子图，用于后续 NCC 比对 |

**说明**：`templateImage` 字段的意义是缓存 ROI 子图，每次检测时无需重新从模板裁剪，直接拿来做 matchTemplate 即可。

### 2.2 DetectionResult —— 单次 ROI 的检测结果

记录某个 ROI 在某张待测图上的检测判定。

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` | `int` | 结果编号，全局递增 |
| `imageName` | `QString` | 待测图片文件名（不含路径） |
| `roiName` | `QString` | 对应检测区域的名称 |
| `isDefective` | `bool` | 是否判定为残缺（true=残缺，false=正常） |
| `defectScore` | `double` | 残缺程度分数，0~1（1 - NCC相似度值，越接近 1 越残缺） |
| `detectionTime` | `QString` | 检测时间戳，格式 `yyyy-MM-dd hh:mm:ss` |

**说明**：一张待测图若有 N 个 ROI，会产生 N 条 DetectionResult。在界面表格中按图片名做"跨行合并"显示。

---

## 三、图像工具模块 —— imageutil.h / imageutil.cpp

这个模块负责 **Qt 与 OpenCV 之间的图像格式转换**、**中文路径的图像读写**、**界面与图像的坐标换算**。

### 3.1 MatToQPixmap —— OpenCV 图像转 Qt 图像

```cpp
QPixmap ImageUtil::MatToQPixmap(const cv::Mat &image);
```

**功能**：把 OpenCV 的 `cv::Mat` 图像转换成 Qt 的 `QPixmap`，以便在 `QLabel` 等控件上显示。

**输入**：
- `image`：OpenCV 图像。支持 BGR 三通道（OpenCV 默认彩色格式）或单通道灰度图。

**输出**：
- `QPixmap`：Qt 图像对象，可直接赋值给 `QLabel::setPixmap()`。

**处理流程**：
1. 若输入是三通道图像 → `cv::cvtColor(COLOR_BGR2RGB)` 转为 RGB（Qt 理解的彩色顺序）
2. 若输入是单通道图像 → 用 `QImage::Format_Grayscale8` 直接构造
3. 用 `QImage(mat.data, ...)` 构造 QImage（零拷贝地共享像素数据）
4. `QPixmap::fromImage()` 转为 QPixmap

---

### 3.2 readImageWithChinesePath —— 中文路径安全读取图像

```cpp
cv::Mat ImageUtil::readImageWithChinesePath(const QString &path);
```

**功能**：解决 Windows 下 `cv::imread()` 无法正确处理中文路径的问题。

**输入**：
- `path`：图像文件路径（可以包含中文）

**输出**：
- `cv::Mat`：成功读取的图像；若文件不存在或解析失败返回空 `Mat`（`.empty() == true`）。

**原理**：
```
文件（磁盘，中文路径）
    ↓  Qt 的 QFile 按字节流读取（QFile 对 UTF-8 路径处理正常）
QByteArray（内存中的原始字节流）
    ↓  cv::imdecode() 直接从字节流解码（不涉及路径，绕过编码问题）
cv::Mat
```

**典型使用场景**：
- 用户通过文件对话框选择中文命名的图片（如"身份证背部.jpg"）
- 直接 `cv::imread(path.toStdString())` 在中文 Windows 上会失败

---

### 3.3 writeImageWithChinesePath —— 中文路径安全写入图像

```cpp
bool ImageUtil::writeImageWithChinesePath(const QString &path, const cv::Mat &image);
```

**功能**：与 `readImageWithChinesePath` 配对，解决保存时的中文路径问题。

**输入**：
- `path`：保存路径（可以包含中文）
- `image`：要保存的 OpenCV 图像

**输出**：
- `bool`：true 表示保存成功，false 表示失败

**原理**：先用 `cv::imencode()` 将图像编码为字节流 `std::vector<uchar>`，再用 Qt 的 `QFile` 写入文件。

---

### 3.4 convertLabelPosToImagePos —— 坐标换算

```cpp
QPoint ImageUtil::convertLabelPosToImagePos(const QPoint &labelPos,
                                             const QSize &labelSize,
                                             const QSize &imageSize);
```

**功能**：把"界面上的鼠标点击位置"换算回"原始图像中的像素位置"。用于 ROI 拖拽框选时的坐标换算。

**输入**：
- `labelPos`：鼠标在 QLabel 上的点击坐标（像素）
- `labelSize`：QLabel 显示区域的尺寸
- `imageSize`：原始图像的尺寸

**输出**：
- `QPoint`：对应在原始图像中的坐标

**换算公式**：
```
ratioX = imageSize.width  / labelSize.width
ratioY = imageSize.height / labelSize.height
imageX = labelX × ratioX
imageY = labelY × ratioY
```

**为什么需要这个换算**：图像在界面上通常是等比例缩放显示的（比如原图 1000×800，在 500×400 的 Label 上显示），用户在界面上框选的矩形只是"显示坐标"，必须换算回原图坐标才能正确裁剪 ROI 子图。

---

## 四、检测算法模块 —— detector.h / detector.cpp ⭐

这是系统的核心算法模块，提供倾斜矫正、模板定位、ROI 精对齐、残缺判定等图像处理函数。

所有函数位于 **`Detector`** 命名空间中。

### 4.1 常量参数集中管理

模块使用结构体静态成员来集中管理算法参数，便于后期调优。

```cpp
struct SkewConstants {
    static constexpr double MIN_ANGLE = 1.0;          // 最小矫正角度（度）
    static constexpr double MAX_LINE_ANGLE = 45.0;     // 最大有效直线角度（度）
    static constexpr int GAUSS_KERNEL = 5;              // 高斯模糊核大小
    static constexpr double CANNY_LOW = 50.0;           // Canny 低阈值
    static constexpr double CANNY_HIGH = 150.0;         // Canny 高阈值
    static constexpr int HOUGH_THRESHOLD = 100;         // HoughLinesP 直线点数阈值
    static constexpr int HOUGH_MAX_GAP = 20;            // HoughLinesP 最大间隔
};

struct DefectConstants {
    static constexpr double NCC_THRESHOLD = 0.85;       // NCC 相似度阈值
    static constexpr double DEFECT_SCORE_THRESHOLD = 0.15; // 残缺分数阈值
    static constexpr int GAUSS_KERNEL = 3;              // 残缺检测前高斯模糊核大小
};
```

---

### 4.2 detectSkewAndCorrect —— 倾斜矫正

```cpp
double Detector::detectSkewAndCorrect(cv::Mat &image);
```

**功能**：检测图像的倾斜角度，若角度超出 MIN_ANGLE（1°）则原地旋转矫正。

**输入**：
- `image`：输入图像，同时作为输出（`in-place` 修改）

**输出**：
- `double`：实际执行的矫正角度（度，正值表示图像原本是顺时针倾斜，返回负角度表示做了逆时针旋转）；0.0 表示无需矫正

**处理流程**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 灰度化 | 彩色 → 灰度；已是灰度则直接克隆 |
| 2 | 高斯模糊 | 5×5 核，压制图像噪声 |
| 3 | Canny 边缘检测 | 低阈值 50、高阈值 150，提取边缘像素 |
| 4 | HoughLinesP 霍夫直线检测 | 搜索边缘图中的直线段；threshold=100 表示一条直线至少需要 100 个边缘点 |
| 5 | 计算角度中位数 | 每条直线算一个角度，取中位数；中位数对异常直线鲁棒 |
| 6 | 判断是否旋转 | 角度绝对值 ≤ 1° 则不旋转（避免无必要的质量损失） |
| 7 | warpAffine 旋转 | 自动扩展画布尺寸并调整平移，确保旋转后图像四角不被裁剪 |

**自动扩展画布原理**：图像旋转后的外接矩形比原图像大（除非旋转 90°/180°）。代码中先计算四个角点旋转后的坐标，再取最大/最小值确定新画布尺寸，然后把仿射变换矩阵的平移部分做相应调整，使旋转后的图像平移到新画布中心。

---

### 4.3 templateMatch —— 模板粗定位

```cpp
cv::Point Detector::templateMatch(const cv::Mat &testImage,
                                   const cv::Mat &templateImage);
```

**功能**：在待测图像上全局搜索与模板图像最相似的位置，返回模板左上角的最佳匹配坐标。

**输入**：
- `testImage`：待测图像（灰度或彩色均可）
- `templateImage`：模板图像

**输出**：
- `cv::Point`：最佳匹配位置的左上角坐标（x, y），即把模板图像贴在该点时与待测图匹配度最高

**核心方法**：`cv::matchTemplate(testImage, templateImage, TM_CCOEFF_NORMED)`
- **TM_CCOEFF_NORMED** = 归一化互相关（Normalized Cross-Correlation, NCC）
- 返回一张"相似度图"，每个像素值表示把模板放在该位置时与待测图的匹配程度，范围 [-1, +1]
- `cv::minMaxLoc()` 在相似度图上找最大值点

**为什么用 NCC（归一化互相关）而不是简单像素差**：
- NCC 对光照变化（整幅图变亮/变暗）不敏感
- 简单像素差 `|A-B|` 对光照变化很敏感，同样一张图亮度稍变就判为差异
- NCC 的本质是只比较"形状/纹理模式"，不比较"绝对亮度"

---

### 4.4 alignROI —— ROI 精对齐

```cpp
cv::Mat Detector::alignROI(const cv::Mat &testImage,
                           const cv::Mat &roiTemplate,
                           const cv::Point &roughMatchPoint,
                           const cv::Rect &roiRect,
                           cv::Rect &outAlignedRect);
```

**功能**：在粗定位的基础上，对单个 ROI 做局部窗口的精细搜索以提高对齐精度。

**输入**：
- `testImage`：待测图像
- `roiTemplate`：该 ROI 的模板子图（即 `ROIRect::templateImage`）
- `roughMatchPoint`：粗定位的匹配点（`templateMatch` 的输出）
- `roiRect`：ROI 在模板图像中的相对坐标

**输出**：
- 返回值 `cv::Mat`：精对齐后从待测图中裁剪的 ROI 子图
- `outAlignedRect`（引用参数）：精对齐后 ROI 在待测图中的矩形坐标

**搜索策略**：
- 以 `(roughMatchPoint.x + roiRect.x, roughMatchPoint.y + roiRect.y)` 为中心
- 搜索窗口大小 = `min(30, roiWidth/3, roiHeight/3)`，即粗定位附近的小窗口
- 在窗口内再次 `matchTemplate` 找最佳匹配点
- 如果搜索窗口过小（ROI 太小）或局部搜索失败，则回退到粗定位位置

**设计动机**：模板粗定位是整幅图的全局搜索，可能有 ±几个像素的误差。局部精细搜索可以把 ROI 对齐精度提高到亚像素级别，直接影响后续残缺判定的准确性。

---

### 4.5 detectCharacterDefect —— 字符残缺判定 ⭐⭐

```cpp
bool Detector::detectCharacterDefect(const cv::Mat &testRegion,
                                      const cv::Mat &templateRegion,
                                      double &defectScore);
```

**功能**：比较待测 ROI 子图与模板 ROI 子图的相似度，判断是否存在印刷残缺。

**输入**：
- `testRegion`：待测图像中精对齐后的 ROI 子图（来自 `alignROI`）
- `templateRegion`：模板图像中对应的 ROI 子图（即 `ROIRect::templateImage`）

**输出**：
- 返回值 `bool`：`true` 表示判定为残缺，`false` 表示正常
- `defectScore`（引用参数）：残缺程度分数，0~1，越接近 1 表示越残缺

**处理流程**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 空值校验 | 若 `testRegion` 或 `templateRegion` 为空 → 直接返回 true（保守判定，保护代码） |
| 2 | 统一转灰度 | 两者都转为单通道灰度图（残缺检测只需亮度信息） |
| 3 | resize 对齐 | 把 `testRegion` resize 到 `templateRegion` 的尺寸，保证逐像素比对 |
| 4 | 高斯模糊 | 3×3 核，压制拍摄抖动、JPEG 压缩噪声，避免正常图被误判 |
| 5 | NCC 相似度比对 | `cv::matchTemplate(testRegion, templateRegion, TM_CCOEFF_NORMED)` |
| 6 | 判定 | `maxVal > 0.85` → 正常；否则 → 残缺。`defectScore = 1 - maxVal` |

**阈值说明**：
- `NCC_THRESHOLD = 0.85` 是经验阈值，可根据实际图像质量调整
- 同一批正常印刷图像的 NCC 值通常在 0.95~1.0 之间；有明显残缺时会降到 0.7 及以下
- `defectScore = 1 - maxVal` 便于在表格中直观展示"残缺程度百分比"

---

## 五、配置管理模块 —— configmanager.h / configmanager.cpp

把"模板图像 + ROI 列表"保存为 JSON 文件，下次直接加载即可，无需重新框选 ROI。

### 5.1 ConfigData —— 配置数据结构体

```cpp
struct ConfigData {
    cv::Mat templateImage;     // 模板图像
    QList<ROIRect> roiList;    // ROI 列表
};
```

### 5.2 saveConfig —— 保存配置

```cpp
bool ConfigManager::saveConfig(const QString &path, const ConfigData &data);
```

**功能**：把配置数据写入 JSON 文件。

**输入**：
- `path`：JSON 文件保存路径
- `data`：配置数据（模板图像 + ROI 列表）

**输出**：
- `bool`：true 保存成功，false 失败

**JSON 文件结构**：

```json
{
    "templateImage": "Base64 编码的模板图像（JPEG 格式）",
    "roiList": [
        {
            "id": 1,
            "x": 100,
            "y": 50,
            "width": 200,
            "height": 80,
            "name": "区域1"
        },
        ...
    ]
}
```

**设计要点**：
1. **模板图像用 Base64 编码**：JSON 是纯文本格式，不能直接存储二进制图像数据。用 `cv::imencode(".jpg", ...)` 把图像编码成二进制字节流，再用 Base64 转为文本嵌入 JSON。
2. **ROI 只存坐标，不存子图**：ROI 的 `templateImage` 子图可以从模板图像 + 坐标重新裁剪得到，不存储可减小文件体积。
3. **用 JPEG 编码**：比 PNG 压缩率更高，Base64 后的文本更短。

### 5.3 loadConfig —— 加载配置

```cpp
bool ConfigManager::loadConfig(const QString &path, ConfigData &data);
```

**功能**：从 JSON 文件读取配置并重建模板图像和 ROI 列表。

**输入**：
- `path`：JSON 文件路径

**输出**：
- `data`（引用参数）：填充好的配置数据
- 返回值 `bool`：true 加载成功，false 失败

**处理流程**：
1. `QFile` 打开文件 → 全部读入 `QByteArray`
2. `QJsonDocument::fromJson()` 解析 JSON
3. 取 `templateImage` 字段的 Base64 文本 → `QByteArray::fromBase64()` → `cv::imdecode()` 得到模板图像
4. 遍历 `roiList` 数组，每个条目创建一个 `ROIRect`，并从模板图像裁剪出 `templateImage` 子图

---

## 六、结果管理模块 —— resultmanager.h / resultmanager.cpp

负责检测结果的界面展示、CSV 导出和清空重置。

所有函数位于 **`ResultManager`** 命名空间中。

### 6.1 addToTable —— 添加结果到表格

```cpp
void ResultManager::addToTable(QTableWidget *table,
                                int imageId,
                                const QList<DetectionResult> &roiResults);
```

**功能**：把一张待测图的所有 ROI 检测结果添加到界面表格。

**输入**：
- `table`：目标表格控件
- `imageId`：图片级序号（用户共检测了第几张图）
- `roiResults`：该图片上所有 ROI 的检测结果列表

**展示特点 —— 跨行合并**：
```
┌────┬────────┬──────────┬──────────┬────────────────────┐
│序号│ 图片名 │ 检测区域 │ 检测结果 │ 检测时间           │
├────┤        ├──────────┼──────────┤                    │
│ 1  │test.jpg│ 区域1    │  正常    │ 2025-06-14 10:30:00│
│    │        ├──────────┼──────────┤                    │ ← 序号、图片名、检测时间
│    │        │ 区域2    │  残缺    │                    │   三列跨行合并
└────┴────────┴──────────┴──────────┴────────────────────┘
```

**实现方式**：
- 在表格末尾插入 N 行（N = ROI 数量）
- 序号、图片名、检测时间三列在 N 行之间用 `QTableWidget::setSpan()` 合并
- 检测区域和检测结果按每个 ROI 逐行填充

**结果列的颜色标记**：
- "残缺" → 红色背景 + 红色文字
- "正常" → 绿色背景 + 绿色文字
- 通过自定义委托 `ResultItemDelegate`（在 mainwindow.cpp 中定义）实现颜色绘制

---

### 6.2 exportToCsv —— 导出 CSV 报告

```cpp
bool ResultManager::exportToCsv(const QString &path,
                                 const QList<DetectionResult> &resultList);
```

**功能**：把全部检测结果导出为 CSV 文件（Excel 可直接打开）。

**输入**：
- `path`：CSV 文件保存路径
- `resultList`：全部检测结果列表

**输出**：
- `bool`：true 成功，false 失败

**CSV 文件内容示例**：
```
序号,图片名称,检测区域,检测结果,残缺程度,检测时间
1,test.jpg,区域1,正常,5.20%,2025-06-14 10:30:00
1,test.jpg,区域2,残缺,28.70%,2025-06-14 10:30:00
2,another.jpg,区域1,正常,3.10%,2025-06-14 10:35:00
...
```

**处理要点**：
- 以 UTF-8 编码写入，保证中文不乱码
- 图片级序号从扁平结果中反向聚合（以 `imageName + detectionTime` 作为 key 维护递增 id）

---

### 6.3 clearAll —— 清空所有结果

```cpp
void ResultManager::clearAll(QTableWidget *table,
                              QList<DetectionResult> &resultList,
                              int &resultCount,
                              int &imageResultCount);
```

**功能**：同时清理内存中的结果数据、两个计数变量和界面表格的所有行。

---

## 七、主窗口与流程编排 —— mainwindow.h / mainwindow.cpp

主窗口是用户交互的入口，也是各算法模块的"调度器"。

### 7.1 主窗口成员变量（关键字段）

| 字段 | 类型 | 含义 |
|------|------|------|
| `m_templateImage` | `cv::Mat` | 已加载的模板图像 |
| `m_roiList` | `QList<ROIRect>` | 用户框选的 ROI 列表 |
| `m_isSelectingROI` | `bool` | 是否正在拖拽框选 ROI |
| `m_roiStartPoint` / `m_roiEndPoint` | `QPoint` | 拖拽的起点/终点 |
| `m_testImage` | `cv::Mat` | 已加载的待测图像 |
| `m_resultImage` | `cv::Mat` | 检测结果图像（画了红/绿框） |
| `m_resultList` | `QList<DetectionResult>` | 全部检测结果 |
| `m_resultCount` | `int` | ROI 级结果计数（全局递增） |
| `m_imageResultCount` | `int` | 图片级计数（检测了几张图） |

### 7.2 工作模式 A —— 模板设计流程

```
┌─────────────────────────────────────────────────────────┐
│  用户点击"加载模板图片"按钮                               │
│    → on_btnLoadTemplate_clicked()                        │
│      → QFileDialog 选择图片                               │
│      → ImageUtil::readImageWithChinesePath() 读取        │
│      → m_templateImage = 读取结果                         │
│      → 在左侧 Label 上显示                                │
├─────────────────────────────────────────────────────────┤
│  用户点击"添加ROI"按钮                                    │
│    → on_btnAddROI_clicked()                              │
│      → 进入拖拽框选模式（m_isSelectingROI = true）        │
│      → 用户在模板图上拖拽一个矩形                          │
│        (mousePressEvent 记录起点)                         │
│        (mouseMoveEvent 实时更新终点，绘制预览矩形)         │
│        (mouseReleaseEvent 根据起终点创建矩形)             │
│      → 弹出 QInputDialog 输入区域名称                     │
│      → 创建 ROIRect 对象，裁剪 templateImage              │
│      → 加入 m_roiList                                    │
│      → 在模板图上用蓝色矩形绘制并更新 ROI 列表             │
├─────────────────────────────────────────────────────────┤
│  用户点击"保存配置文件"按钮                                │
│    → on_btnSaveConfig_clicked()                          │
│      → ConfigManager::saveConfig(path, {template, roiList})│
│      → 保存为 JSON 文件                                  │
└─────────────────────────────────────────────────────────┘
```

### 7.3 工作模式 B —— 图片检测流程

```
┌─────────────────────────────────────────────────────────┐
│  用户点击"加载配置文件"按钮                                │
│    → on_btnLoadConfig_clicked()                         │
│      → 选择 JSON 配置文件                                │
│      → ConfigManager::loadConfig(path, data)            │
│      → m_templateImage = data.templateImage             │
│      → m_roiList = data.roiList                        │
│      → 在左侧 Label 上显示模板图 + ROI 蓝色框             │
├─────────────────────────────────────────────────────────┤
│  用户点击"加载待测图片" / "开始检测"按钮                    │
│    → performDetection(path)                             │
│      ├─ ImageUtil::readImageWithChinesePath() → testMat │
│      ├─ Detector::detectSkewAndCorrect(testMat)         │
│      ├─ cv::Point matchPos = Detector::templateMatch(   │
│      │                       testMat, m_templateImage)  │
│      ├─ m_resultImage = testMat.clone()                 │
│      ├─ 对 m_roiList 中的每个 roi:                       │
│      │    ├─ cv::Rect alignedRect                       │
│      │    ├─ cv::Mat testROI = Detector::alignROI(      │
│      │    │                  testMat, roi.templateImage,│
│      │    │                  matchPos, roi.rect, alignedRect)│
│      │    ├─ double defectScore = 0.0                   │
│      │    ├─ bool defective = Detector::detectCharacterDefect(│
│      │    │                  testROI, roi.templateImage,│
│      │    │                  defectScore)               │
│      │    ├─ 创建 DetectionResult 并加入 m_resultList    │
│      │    └─ 在 m_resultImage 上画矩形                   │
│      │       (绿色=正常，红色=残缺，带名称标注)            │
│      └─ ResultManager::addToTable(table, imageId, perImageResults)│
│      → 在右侧 Label 上显示 m_resultImage                 │
├─────────────────────────────────────────────────────────┤
│  用户点击"导出结果"按钮                                    │
│    → on_btnExportResults_clicked()                       │
│      → QFileDialog 选择 CSV 保存路径                     │
│      → ResultManager::exportToCsv(path, m_resultList)    │
│      → 弹出"导出成功"提示                                │
├─────────────────────────────────────────────────────────┤
│  用户点击"清空结果"按钮                                    │
│    → on_btnClearResults_clicked()                        │
│      → ResultManager::clearAll(table, m_resultList,      │
│      │                   m_resultCount, m_imageResultCount)│
│      → 界面表格、内存数据、计数全部重置                   │
└─────────────────────────────────────────────────────────┘
```

### 7.4 performDetection —— 核心检测调度函数

这是单次检测的"总入口函数"，内部按顺序调用各算法模块，是理解整个系统运行流程的关键。

```cpp
void MainWindow::performDetection(const QString &imagePath)
{
    // 1. 读取待测图像（中文路径安全）
    cv::Mat testMat = ImageUtil::readImageWithChinesePath(imagePath);
    if (testMat.empty()) {
        QMessageBox::warning(this, "警告", "无法读取待测图像");
        return;
    }

    // 2. 倾斜矫正（原地修改 testMat）
    Detector::detectSkewAndCorrect(testMat);

    // 3. 模板粗定位：在待测图上找模板最佳匹配位置
    cv::Point matchPos = Detector::templateMatch(testMat, m_templateImage);

    // 4. 克隆一份用于绘制检测结果
    m_resultImage = testMat.clone();
    QList<DetectionResult> perImageResults;

    // 5. 对每个 ROI 独立检测
    for (const ROIRect &roi : m_roiList) {
        // 5a. ROI 精对齐
        cv::Rect alignedRect;
        cv::Mat testROI = Detector::alignROI(testMat, roi.templateImage,
                                               matchPos, roi.rect, alignedRect);

        // 5b. NCC 残缺判定
        double defectScore = 0.0;
        bool isDefective = Detector::detectCharacterDefect(
            testROI, roi.templateImage, defectScore);

        // 5c. 构造检测结果
        DetectionResult result;
        result.id = ++m_resultCount;
        result.imageName = QFileInfo(imagePath).fileName();
        result.roiName = roi.name;
        result.isDefective = isDefective;
        result.defectScore = defectScore;
        result.detectionTime = QDateTime::currentDateTime().toString("yyyy-MM-dd hh:mm:ss");
        m_resultList.append(result);
        perImageResults.append(result);

        // 5d. 在结果图像上绘制矩形框（绿色=正常，红色=残缺）
        cv::Scalar color = isDefective ? cv::Scalar(0, 0, 255) : cv::Scalar(0, 255, 0);
        cv::rectangle(m_resultImage, alignedRect, color, 2);
        // 旁边绘制 ROI 名称文本
        std::string label = roi.name.toStdString();
        cv::putText(m_resultImage, label,
                    cv::Point(alignedRect.x, alignedRect.y - 5),
                    cv::FONT_HERSHEY_SIMPLEX, 0.5, color, 1, cv::LINE_AA);
    }

    // 6. 增加图片级计数，更新表格显示
    m_imageResultCount++;
    ResultManager::addToTable(ui->tableResults, m_imageResultCount, perImageResults);

    // 7. 在右侧 Label 上显示带标注的结果图像
    ui->labelTestImage->setPixmap(ImageUtil::MatToQPixmap(m_resultImage));
}
```

---

## 八、程序入口 —— main.cpp

```cpp
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);

    // 加载 QSS 样式表（美化界面：按钮、表格、标签等的外观）
    QFile styleFile(":/style.qss");
    if (styleFile.open(QFile::ReadOnly)) {
        QString styleSheet = QLatin1String(styleFile.readAll());
        a.setStyleSheet(styleSheet);
        styleFile.close();
    }

    // 创建主窗口并显示
    MainWindow w;
    w.show();

    // 进入 Qt 事件循环（直到窗口关闭）
    return a.exec();
}
```

**关键点**：
- `QApplication` 是每个 Qt 应用必须创建的全局应用对象
- `:/style.qss` 中的冒号表示从 Qt 资源系统读取（资源在 `resources.qrc` 中定义，编译时嵌入可执行文件）
- `exec()` 阻塞直到主窗口关闭，返回应用退出码

---

## 九、关键参数速查表

| 参数 | 所在模块 | 值 | 含义 |
|------|---------|-----|------|
| `SkewConstants::MIN_ANGLE` | detector.cpp | 1.0° | 小于此角度不旋转矫正，避免质量损失 |
| `SkewConstants::GAUSS_KERNEL` | detector.cpp | 5 | 倾斜矫正前高斯模糊核大小 |
| `SkewConstants::CANNY_LOW` | detector.cpp | 50 | Canny 边缘检测低阈值 |
| `SkewConstants::CANNY_HIGH` | detector.cpp | 150 | Canny 边缘检测高阈值 |
| `SkewConstants::HOUGH_THRESHOLD` | detector.cpp | 100 | 霍夫直线检测最小边缘点数 |
| `DefectConstants::NCC_THRESHOLD` | detector.cpp | 0.85 | NCC 相似度低于此值判为残缺 |
| `DefectConstants::GAUSS_KERNEL` | detector.cpp | 3 | 残缺检测前高斯模糊核大小 |

以上参数均可在 `detector.cpp` 顶部修改以调整检测效果。

---

## 十、补充：自定义绘制委托 ResultItemDelegate

在 `mainwindow.cpp` 中定义了一个自定义的 `QStyledItemDelegate` 子类，用于给表格中"检测结果"列上色：

```cpp
void ResultItemDelegate::paint(QPainter *painter,
                                const QStyleOptionViewItem &option,
                                const QModelIndex &index) const
{
    QString text = index.data(Qt::DisplayRole).toString();

    // 根据内容选择颜色
    QColor bgColor, textColor;
    if (text == "残缺") {
        bgColor = QColor(255, 200, 200);   // 浅红背景
        textColor = QColor(180, 0, 0);      // 深红文字
    } else if (text == "正常") {
        bgColor = QColor(200, 255, 200);    // 浅绿背景
        textColor = QColor(0, 150, 0);      // 深绿文字
    } else {
        QStyledItemDelegate::paint(painter, option, index);
        return;
    }

    // 绘制背景
    painter->fillRect(option.rect, bgColor);

    // 绘制居中文字
    painter->setPen(textColor);
    painter->drawText(option.rect, Qt::AlignCenter, text);
}
```

在 MainWindow 构造函数中通过 `ui->tableResults->setItemDelegateForColumn(3, new ResultItemDelegate(this))` 把这个自定义委托绑定到"检测结果"列。这样该列单元格在绘制时会走自定义 paint() 而不是默认绘制。
